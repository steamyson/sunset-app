---
phase: quick
plan: 260404-0gp
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(tabs)/chats.tsx
  - app/room/[code].tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Sky header displays '{name}'s Sky' using the user's own stored nickname"
    - "New room creation modal shows a text input to name the cloud before closing"
    - "Entering the room screen immediately shows the cloud name in the header without waiting for async load"
  artifacts:
    - path: "app/(tabs)/chats.tsx"
      provides: "userName state, cloud naming TextInput, name param on router.push"
    - path: "app/room/[code].tsx"
      provides: "name param in search params type, nickname initialized from nav param"
  key_links:
    - from: "app/(tabs)/chats.tsx"
      to: "utils/identity.ts"
      via: "getLocalNickname() in useEffect"
      pattern: "getLocalNickname"
    - from: "app/(tabs)/chats.tsx"
      to: "app/room/[code].tsx"
      via: "router.push params.name"
      pattern: "name: nicknames"
---

<objective>
Three targeted UI changes: (1) the sky canvas header reads "{Name}'s Sky" using the user's stored nickname, (2) the new room creation modal adds a cloud-naming text input, (3) the room screen header immediately shows the cloud name by receiving it as a nav param instead of waiting for async fetch.

Purpose: Improves personalization and reduces the flash of generic content on room entry.
Output: Two modified files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260404-0gp-sky-header-shows-users-name-cloud-naming/260404-0gp-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sky header personalization and cloud naming input in chats.tsx</name>
  <files>app/(tabs)/chats.tsx</files>
  <action>
Two changes in this file:

**Change 1 — Sky header:**
- Import `getLocalNickname` from `utils/identity` (add to existing import line or add new import).
- Add state: `const [userName, setUserName] = useState<string | null>(null);` near other useState declarations.
- In the existing mount useEffect (or a new one), call `getLocalNickname().then(n => setUserName(n))`.
- At line ~1265 where `"Your Sky"` is rendered, replace the string with `{userName ? \`${userName}'s Sky\` : "Your Sky"}`.

**Change 2 — Cloud naming on room creation:**
- Add state: `const [newCloudName, setNewCloudName] = useState("");` near other modal states.
- In the `newlyCreatedCode` block (lines ~1340–1356), add a `TextInput` between the code box and the Share Code button:
  - Placeholder: `"Name this cloud (optional)"`
  - `value={newCloudName}`
  - `onChangeText={setNewCloudName}`
  - Style: same width as the code box, border from `theme.colors`, text centered, use colors from `utils/theme.ts`.
- Add a "Save Name" `TouchableOpacity` button below the TextInput (above or replacing nothing — keep Share Code and Done as-is). On press:
  - If `newCloudName.trim()` is non-empty: call `setRoomNickname(newlyCreatedCode, newCloudName.trim())` then update local state `setNicknames(prev => ({ ...prev, [newlyCreatedCode]: newCloudName.trim() }))`.
  - Always close the modal after saving (set `setNewlyCreatedCode(null)` or equivalent close logic).
- When the modal fully closes (wherever `newlyCreatedCode` is reset to null/empty), also reset `setNewCloudName("")`.

**Change 3 — Pass name param on router.push:**
- Find all `router.push({ pathname: "/room/[code]", params: { code: room.code, ... } })` calls in chats.tsx.
- Add `name: nicknames[room.code] ?? ""` to each params object.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>TypeScript passes. Sky header shows personalized name when nickname is set. Cloud naming input appears in the new room modal. All router.push calls to room include the name param.</done>
</task>

<task type="auto">
  <name>Task 2: Room screen accepts name nav param and pre-populates header</name>
  <files>app/room/[code].tsx</files>
  <action>
- At line ~112, extend the `useLocalSearchParams` generic type: `useLocalSearchParams<{ code: string; unread?: string; name?: string }>()`.
- Destructure `name` from the result alongside `code` and `unread`.
- At line ~126, change the `nickname` useState initializer from `null` to `params.name || null` — or equivalently `name ? name : null` using the destructured value.

No other changes needed. The existing conditional header (lines ~732–748) already renders the large name + small code layout when `nickname` is non-null, so pre-populating from the nav param is sufficient.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>TypeScript passes. Navigating to a named room immediately shows the cloud name in the header without waiting for the async nickname fetch.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npx tsc --noEmit` passes with no errors.
- Manual spot-check: open app, sky header shows user's nickname + "'s Sky". Create a room, type a name, press Save — cloud is named immediately. Tap a named cloud, room header shows name instantly.
</verification>

<success_criteria>
- No TypeScript errors.
- Sky header reflects stored nickname.
- New room modal has functional cloud naming input.
- Room header pre-populated from nav param on entry.
</success_criteria>

<output>
After completion, create `.planning/quick/260404-0gp-sky-header-shows-users-name-cloud-naming/260404-0gp-SUMMARY.md`
</output>
