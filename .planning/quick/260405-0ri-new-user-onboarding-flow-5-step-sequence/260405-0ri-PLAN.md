---
phase: quick
plan: 260405-0ri
type: execute
wave: 1
depends_on: []
files_modified:
  - app/onboarding.tsx
  - app/_layout.tsx
autonomous: true
must_haves:
  truths:
    - "New user sees 5-step onboarding after setup (nickname set, onboarding not complete)"
    - "User can set profile photo or skip"
    - "Completing or skipping onboarding sets onboarding_complete in SecureStore"
    - "Returning users who completed onboarding go straight to /home"
    - "Step transitions animate with fade+translate, dots track progress"
  artifacts:
    - path: "app/onboarding.tsx"
      provides: "5-step onboarding screen"
      min_lines: 200
    - path: "app/_layout.tsx"
      provides: "onboarding gate in init()"
      contains: "onboarding_complete"
  key_links:
    - from: "app/_layout.tsx"
      to: "app/onboarding.tsx"
      via: "router.replace('/onboarding') when onboarding_complete absent"
      pattern: "router\\.replace.*onboarding"
    - from: "app/onboarding.tsx"
      to: "utils/storage.ts"
      via: "setItem('onboarding_complete', 'true')"
      pattern: "setItem.*onboarding_complete"
---

<objective>
Create a 5-step onboarding flow for new users, gated in the app layout so it runs once after initial nickname setup.

Purpose: First-run experience that introduces dusk concepts (clouds, golden hour) and optionally captures a profile photo before the user reaches the sky canvas.
Output: `app/onboarding.tsx` with all 5 steps, gate logic in `app/_layout.tsx`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/_layout.tsx
@components/Text.tsx
@components/SkyCloud.tsx
@utils/storage.ts
@utils/theme.ts
@utils/avatar.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create onboarding screen with all 5 steps</name>
  <files>app/onboarding.tsx</files>
  <action>
Create `app/onboarding.tsx` with a single component managing a `step` state (0-4).

**Layout structure:**
- Outer `View` fills screen, background `colors.cream`
- Content area (flex: 1) renders current step content
- Bottom bar: 5 step dots + skip link

**Step dots:**
- 5 dots in a horizontal row, centered
- Active dot: width 18, height 8, borderRadius 4, backgroundColor `colors.ember`
- Inactive dot: width 8, height 8, borderRadius 4, backgroundColor `colors.mist`
- Animate width with `Animated.Value` per dot, spring to target on step change

**Step transitions:**
- Each step's content wrapped in an `Animated.View` with opacity and translateY
- On step change: reset opacity to 0, translateY to 18, then `Animated.parallel` both to (1, 0) with timing duration 400ms, `useNativeDriver: true`
- Use a single pair of `Animated.Value`s, reset them on step change via `setValue()`

**Skip link:**
- Text "skip" in `colors.ash`, positioned bottom-right of dot bar area
- On press: `setItem("onboarding_complete", "true")` then `router.replace("/home")`

**Step 0 — Welcome:**
- `// TODO: replace with LinearGradient from expo-linear-gradient` comment above the container
- Solid `colors.cream` background
- Center: "dusk." in large font (~40px), `colors.charcoal`, bold — fades in (opacity 0->1, 600ms)
- Below: "every sunset, once." smaller (~18px), `colors.ash` — fades in with 200ms delay
- Bottom: "tap to continue" in `colors.mist`, small font
- Tap anywhere on the step to advance (wrap in Pressable)

**Step 1 — Profile photo:**
- Large circle (W * 0.45 diameter), dashed border (2px, `colors.mist`), centered
- If no photo selected: camera icon placeholder (use a simple Text emoji or SVG circle)
- Prompt text: "put a face to the sky" in `colors.charcoal`
- Two buttons stacked vertically:
  - "take photo" — solid background `colors.ember`, white text, rounded (borderRadius 24)
  - "choose from library" — outlined, border `colors.ember`, text `colors.ember`, rounded
- "skip for now" ghost link below in `colors.ash`
- Use `expo-image-picker`: `launchCameraAsync` for take photo, `launchImageLibraryAsync` for choose
  - Options: `mediaTypes: "images"`, `allowsEditing: true`, `aspect: [1, 1]`, `quality: 0.8`
- On photo selected: `setItem("profile_photo_uri", uri)` and show photo in the circle (Image component, borderRadius to make circular)
- After photo shows, auto-advance to step 2 after 600ms delay (setTimeout)
- "skip for now" advances to step 2 without storing

**Step 2 — How clouds work:**
- Import `SkyCloud` from `components/SkyCloud.tsx`
- Render `SkyCloud` with `variant={2}`, `width={W * 0.54}`, `hideLabel={false}`, `name="your room"`
- Wrap in `Animated.View` with `translateX` animated value
- `Animated.loop(Animated.sequence([timing to 30, 3000ms], [timing to -30, 3000ms]))`, `useNativeDriver: true`
- Behind it: second `SkyCloud` same props but `opacity: 0.25`, static position offset slightly
  - This ghost cloud fades in (opacity 0->0.25) over 800ms after step enters
- Text below: "rooms are clouds. each one holds a group and their photos. tap to enter. drag to move them around your sky."
  - `colors.charcoal`, ~15px, centered, line height ~22
- Tap/continue button at bottom to advance

**Step 3 — Golden hour:**
- Text: "photos only live during golden hour — 90 minutes before to 45 minutes after sunset. then they're gone."
  - `colors.charcoal`, ~15px, centered
- Static countdown badge: a rounded rect with `colors.ember` background, showing "1:23:45" as example text in white, bold
  - Below badge: "until golden hour" in `colors.ash`
- Decorative: small sun circle (`colors.ember`, width 60, height 60, borderRadius 30) positioned above the text, opacity 0.6
- Tap to advance

**Step 4 — Ready:**
- "your sky is waiting." centered, larger font (~28px), `colors.charcoal`
- Spring-in animation: scale Animated.Value from 0.9 to 1, opacity 0 to 1
  - `Animated.spring` with `tension: 120, friction: 8, useNativeDriver: true`
- Button: "open dusk" — solid `colors.ember` background, white text, large rounded (borderRadius 28), paddingHorizontal 48, paddingVertical 16
- On press: `setItem("onboarding_complete", "true")` then `router.replace("/home")`

**All text:** Use `components/Text.tsx`, never RN Text. All copy lowercase. Minimal punctuation as shown above.

**All colors:** From `utils/theme.ts` — no hardcoded hex values.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>app/onboarding.tsx exists with all 5 steps rendering, type-checks clean, animations configured, profile photo picker functional, step dots track progress, skip path works</done>
</task>

<task type="auto">
  <name>Task 2: Gate onboarding in layout init</name>
  <files>app/_layout.tsx</files>
  <action>
In `app/_layout.tsx`, modify the `init()` function:

1. After the existing nickname check (the `if (!nickname)` block that routes to `/setup`), add:
   ```
   const onboardingDone = await getItem("onboarding_complete");
   if (!onboardingDone) {
     router.replace("/onboarding");
     return;
   }
   ```
2. This goes BEFORE the existing `router.replace("/home")` call
3. Import `getItem` from `utils/storage` if not already imported (it likely is — check first)
4. Flow becomes: no nickname -> /setup, no onboarding -> /onboarding, else -> /home
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>New users without onboarding_complete are routed to /onboarding. Users who completed onboarding go to /home. Existing users with nickname but no onboarding key will see onboarding once (acceptable for existing testers — they can skip).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Manual: fresh install flow -> setup nickname -> lands on onboarding step 0
- Manual: complete all 5 steps -> lands on /home
- Manual: skip at any point -> lands on /home
- Manual: relaunch app -> goes straight to /home (onboarding_complete set)
</verification>

<success_criteria>
- Onboarding screen renders all 5 steps with correct content and animations
- Step transitions animate smoothly (fade + translateY)
- Profile photo picker works (camera + library)
- Step dots accurately reflect current step
- Skip and completion both set onboarding_complete and route to /home
- Layout gate correctly routes new users through onboarding
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/quick/260405-0ri-new-user-onboarding-flow-5-step-sequence/260405-0ri-SUMMARY.md`
</output>
