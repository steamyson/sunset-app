---
phase: quick-260409-jx3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [utils/messages.ts]
autonomous: true
requirements: [fix-saveToMyMap-raw-path]

must_haves:
  truths:
    - "saveToMyMap stores the raw storage path (e.g. 'device123/photo.jpg') not a public URL"
    - "getLocalMapPins pipes through mapWithSignedPhotoUrls so pins display correctly"
  artifacts:
    - path: "utils/messages.ts"
      provides: "saveToMyMap function with raw path storage"
      contains: "photo_url: photoPath"
  key_links:
    - from: "utils/messages.ts saveToMyMap"
      to: "utils/photosStorage.ts mapWithSignedPhotoUrls"
      via: "getLocalMapPins already calls mapWithSignedPhotoUrls on read"
      pattern: "mapWithSignedPhotoUrls"
---

<objective>
Remove the getPublicUrl call from saveToMyMap so it stores the raw storage path instead of a permanent public URL. Every other code path in the app stores raw paths and signs them on read via mapWithSignedPhotoUrls -- this is the single inconsistency.

Purpose: Align saveToMyMap with the rest of the codebase's signed-URL-on-read pattern.
Output: Updated utils/messages.ts
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@utils/messages.ts (lines 175-201 -- saveToMyMap function)
@utils/photosStorage.ts (mapWithSignedPhotoUrls already handles raw paths)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store raw path in saveToMyMap</name>
  <files>utils/messages.ts</files>
  <action>
In saveToMyMap (around line 185):
1. Remove the line: `const { data: urlData } = supabase.storage.from("photos").getPublicUrl(photoPath);`
2. Change `photo_url: urlData.publicUrl` to `photo_url: photoPath`
3. If the supabase import from this file is only used by that getPublicUrl call, check other usages before removing. (It is likely used elsewhere -- leave the import.)

No other changes needed. getLocalMapPins already calls mapWithSignedPhotoUrls on the returned pins.
  </action>
  <verify>
    <automated>cd C:/Users/akiva/.Sunset/Dusk && npx tsc --noEmit</automated>
  </verify>
  <done>saveToMyMap sets photo_url to the raw storage path (photoPath) with no getPublicUrl call. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
npx tsc --noEmit passes with no errors.
Grep confirms no getPublicUrl call remains in saveToMyMap.
</verification>

<success_criteria>
- saveToMyMap stores raw path, not public URL
- TypeScript compiles without errors
- No other code changes needed (getLocalMapPins already handles signing)
</success_criteria>

<output>
After completion, create `.planning/quick/260409-jx3-fix-savetomymap-to-store-raw-path-not-pu/260409-jx3-SUMMARY.md`
</output>
