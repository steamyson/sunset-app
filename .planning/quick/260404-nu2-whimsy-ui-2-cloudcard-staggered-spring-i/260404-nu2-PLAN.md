---
phase: quick
plan: 260404-nu2
type: execute
wave: 1
depends_on: []
files_modified: [components/CloudCard.tsx]
autonomous: true
requirements: [whimsy-ui-2]

must_haves:
  truths:
    - "CloudCards spring in on mount with staggered timing based on seed prop"
    - "Animation is scale 0.88->1 + opacity 0->1"
    - "Delay between cards is seed * 60ms"
  artifacts:
    - path: "components/CloudCard.tsx"
      provides: "Spring-in entry animation on mount"
      contains: "Animated.spring"
  key_links:
    - from: "components/CloudCard.tsx"
      to: "seed prop"
      via: "delay = seed * 60"
      pattern: "seed.*60"
---

<objective>
Add a staggered spring-in entry animation to CloudCard so cards pop in sequentially on mount.

Purpose: Whimsy UI pass -- cards feel alive instead of appearing instantly.
Output: Updated CloudCard.tsx with mount animation.
</objective>

<context>
@components/CloudCard.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spring-in mount animation to CloudCard</name>
  <files>components/CloudCard.tsx</files>
  <action>
In CloudCard.tsx:

1. Import `useRef, useEffect` from React and `Animated` from react-native.

2. Inside the CloudCard function, create a single `Animated.Value` ref initialized to 0:
   ```
   const anim = useRef(new Animated.Value(0)).current;
   ```

3. In a `useEffect` (empty deps), run a delayed spring:
   ```
   const numSeed = typeof seed === 'string' ? seed.length : seed;
   const delay = numSeed * 60;
   const timeout = setTimeout(() => {
     Animated.spring(anim, {
       toValue: 1,
       tension: 120,
       friction: 8,
       useNativeDriver: true,
     }).start();
   }, delay);
   return () => clearTimeout(timeout);
   ```

4. Derive interpolated values:
   - `scale`: anim interpolate 0->0.88, 1->1
   - `opacity`: anim (already 0->1)

5. Change the outer `View` to `Animated.View` and apply:
   ```
   style={[{ marginTop: 12, opacity: anim, transform: [{ scale }] }, style]}
   ```

6. Keep the inner View unchanged (shadow, borderRadius, bg, innerStyle).

Do NOT use Reanimated -- RN Animated only. useNativeDriver: true (scale+opacity are native-compatible). Standard spring config per CLAUDE.md: tension 120, friction 8.
  </action>
  <verify>
    <automated>cd C:/Users/akiva/.Sunset/Dusk && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>CloudCards animate in on mount with staggered delay. TypeScript compiles clean. Profile screen cards pop in sequentially (seed 0 = instant, seed 1 = 60ms delay, etc.).</done>
</task>

</tasks>

<verification>
npx tsc --noEmit passes with no errors on CloudCard.tsx changes.
</verification>

<success_criteria>
- CloudCard.tsx uses RN Animated with useNativeDriver: true
- Spring config is tension: 120, friction: 8
- Scale interpolates 0.88 -> 1, opacity 0 -> 1
- Delay is seed * 60ms
- No new packages added
- TypeScript strict passes
</success_criteria>

<output>
After completion, create `.planning/quick/260404-nu2-whimsy-ui-2-cloudcard-staggered-spring-i/260404-nu2-SUMMARY.md`
</output>
