---
phase: 01-globe
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified: []
autonomous: false
requirements:
  - GLOB-01
  - GLOB-02
  - GLOB-03

must_haves:
  truths:
    - "Globe shows continent line art that rotates with drag"
    - "Deeper zoom makes the globe visually larger"
    - "Clouds drift independently on the globe surface"
    - "Pinch out returns to sky canvas"
    - "No gesture regressions (cloud tap, sky pan, pinch zoom all work)"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of all Phase 1 globe features on a device or simulator.

Purpose: Confirm the globe zoom extension, continent art, and rotation behavior all work correctly before marking the phase complete.
Output: User confirmation that all three requirements (GLOB-01, GLOB-02, GLOB-03) are satisfied.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-globe/01-CONTEXT.md
@.planning/phases/01-globe/01-01-SUMMARY.md
@.planning/phases/01-globe/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify globe zoom, continent art, and rotation</name>
  <files>app/(tabs)/chats.tsx</files>
  <action>
Present the verification checklist to the user. No code changes needed — this is a visual verification checkpoint.

Launch the app with `npm start` and ask the user to test on their device or simulator. Walk them through the verification steps in how-to-verify.
  </action>
  <verify>User confirms "approved" or provides feedback for fixes</verify>
  <done>User has verified all three GLOB requirements work correctly on device/simulator with no gesture regressions.</done>
  <what-built>
    Extended globe zoom (floor lowered from 0.35 to 0.18), continent line art (7 simplified continent blobs with fill and stroke), globe rotation (continents rotate with drag), cloud back-face hiding (fully invisible behind globe), and independent cloud drift.
  </what-built>
  <how-to-verify>
    1. Launch the app: `npm start` and open on device/simulator
    2. Navigate to the Chats tab (sky canvas)
    3. Tap the globe button to enter globe view — globe appears at zoom 0.35

    **GLOB-01 (Deeper zoom):**
    4. Pinch inward (zoom deeper) — the globe should grow larger on screen as you approach zoom 0.18
    5. Confirm the globe does NOT snap back to sky view while zooming between 0.18-0.55
    6. Pinch outward past 0.78 — should snap back to sky canvas view

    **GLOB-02 (Continent art):**
    7. Look at the globe surface — you should see simplified continent blob shapes
    8. Continents should have a slightly lighter fill (#1e4a72) than the ocean (#1a3a5c) with soft blue-white stroke outlines
    9. No glow or shadow on the strokes — clean crisp lines only
    10. The shapes should be recognizable as rough continent blobs (not detailed coastlines)

    **GLOB-03 (Rotation + independent clouds):**
    11. Single-finger drag on the globe — continent shapes should rotate in sync with your drag
    12. After releasing, continent shapes should drift with momentum decay
    13. Room clouds (if present) should orbit slowly and independently of the continents
    14. Clouds on the back side of the globe should be fully invisible (not partially visible)

    **Regression checks:**
    15. Sky canvas: verify cloud taps still enter rooms
    16. Sky canvas: verify sky pan (single-finger drag) still works
    17. Sky canvas: verify pinch zoom transitions between sky and globe modes smoothly
  </how-to-verify>
  <acceptance_criteria>
    - User types "approved" confirming all GLOB-01, GLOB-02, GLOB-03 behaviors work
    - No gesture regressions reported (cloud tap, sky pan, pinch zoom all functional)
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
User confirms all three requirements work correctly on device/simulator.
</verification>

<success_criteria>
- User approves the visual behavior of the globe zoom, continent art, and rotation
- No gesture regressions reported
</success_criteria>

<output>
After completion, create `.planning/phases/01-globe/01-03-SUMMARY.md`
</output>
